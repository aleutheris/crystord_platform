// This file name conflicts with the VS Code Apollo extension's config loader.
// The real Angular Apollo client options are defined in `apollo-angular.config.ts`.
// We intentionally export nothing here so the extension falls back to the root `apollo.config.cjs`.
export {};
