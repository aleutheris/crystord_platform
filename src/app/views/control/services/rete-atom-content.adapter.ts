// Deprecated: the project now relies on Rete's built-in ClassicPreset.InputControl for
// editing atom content directly on nodes. This file is retained as an empty module to
// avoid breaking import paths in older builds but intentionally exports nothing.

export {};
