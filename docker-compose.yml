version: '3.8'

services:
  crystord_web:
    image: crystord_web
    container_name: crystord_web
    ports:
      - "4201:4201"
    networks:
      - default
      - reverse_proxy_default
    restart: unless-stopped

networks:
  default:
    driver: bridge
  reverse_proxy_default:
    external: true
